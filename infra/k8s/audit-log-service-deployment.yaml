apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-log-service
  namespace: evidentia
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audit-log-service
  template:
    metadata:
      labels:
        app: audit-log-service
    spec:
      containers:
      - name: audit-log-service
        image: <ACR_NAME>.azurecr.io/audit-log-service:latest
        ports:
        - containerPort: 8081
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: audit-db-secret
              key: url
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: audit-log-service
  namespace: evidentia
spec:
  selector:
    app: audit-log-service
  ports:
  - port: 80
    targetPort: 8081
  type: ClusterIP
